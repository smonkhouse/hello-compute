{
  "dependencies": {
    "@fastly/js-compute": "^3.30.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "lucide-react": "^0.378.0"
  },
  "devDependencies": {
    "@fastly/cli": "^10.14.0",
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.2.0",
    "tailwindcss": "^3.4.3",
    "postcss": "^8.4.38",
    "autoprefixer": "^10.4.19"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:fastly": "js-compute-runtime src/index.js bin/main.wasm",
    "start": "fastly compute serve --watch",
    "deploy": "npm run build && fastly compute publish --accept-defaults --auto-yes",
    "domain": "fastly domain list --version=latest"
  }
}
